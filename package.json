{
  "name": "trackio-analytics-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:tracker": "javascript-obfuscator public/js/tracker.js --output public/js/tracker.min.js --compact true --string-array true --string-array-encoding base64 --control-flow-flattening true",
    "start": "npm run build:tracker && node server.js",
    "dev": "npm run build:tracker && nodemon server.js",
    "upload": "git diff --quiet && git diff --cached --quiet && echo 'No changes to commit' || (git add . && git commit -m \"auto-update\" && git push origin main)",
    "download": "git fetch origin && git reset --hard origin/main && git clean -fd && echo 'Project updated from GitHub'"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-rate-limit": "^6.7.0",
    "express-session": "^1.17.3",
    "helmet": "^7.0.0",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^7.0.0",
    "morgan": "^1.10.0",
    "nanoid": "^4.0.2",
    "passport": "^0.6.0",
    "passport-google-oauth20": "^2.0.0"
  },
  "devDependencies": {
    "javascript-obfuscator": "^5.3.0",
    "nodemon": "^2.0.22"
  },
  "keywords": [
    "analytics",
    "tracker",
    "monitoring",
    "real-time",
    "backend"
  ],
  "author": "Sohaib Ali",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sohaibalidev/trackio-analytics-server.git"
  }
}
